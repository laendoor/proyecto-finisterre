# Bitácora — 2026-06-10

## Sesión: feat/auth-invitaciones-7

## Resumen

Sesión de arranque del ticket #7 (Auth — invitaciones). Se corrigieron dos skills de workflow (fini-ticket y el nuevo ciclo discutir/implementar-fase) y se implementó la Fase Alpha del ticket: seed de admin con CLI interactivo.

## Trabajo realizado

### proyecto-finisterre

Sin cambios.

### academika (branch: `feat/auth-invitaciones-7`)

**Fase Alpha — Seed de admin:**

- `api/app/seeds/create_admin.py` — script CLI interactivo: pide email + contraseña (getpass), reutiliza `hash_password`, inserta en `users` con `role='admin'`. Rechaza emails duplicados.
- `Makefile` — nuevo target `seed-admin` + descripción en `make help`
- `README.md` — creado desde cero: qué es, requisitos, quick start, tabla de seeds, `make help`

### Workspace / configuración

**fini-ticket** (skill existente, actualizado):

- Agrega `cd ~/unq/seminario/academika` después de crear el branch, para que el status line de Claude Code muestre el estado del repo correcto
- Mueve el suggest de `/rename` al final del último mensaje del skill (antes estaba inmediatamente después del branch, fácil de pasar por alto)
- Agrega `TaskCreate` por fase en Paso 5 para panel de progreso en sidebar

**fini-discutir-fase** (skill nuevo):

- Paso 5 es gate duro: actualizar `plan-activo.md` con el detalle acordado antes de dar stand-by, sin excepciones
- Agrega `TaskUpdate` para marcar la fase como discutida en el panel

**fini-implementar-fase** (skill nuevo):

- Check de `discutida ✓` es DETENER absoluto — no hay bypass por "la discusión fue verbal en la sesión"
- Agrega `TaskUpdate` al completar la implementación

## Decisiones tomadas

- **Seed de admin**: CLI interactivo (`make seed-admin`) en lugar de hash hardcodeado en SQL o runner Python. Funciona igual en dev y prod; el password nunca toca el repo. Requiere haber corrido `make seed` antes (la FK a `lkp_user_roles` falla con error descriptivo si no).
- **Workflow fix**: el detalle de fase discutida debe escribirse en `plan-activo.md` _antes_ de implementar. TaskCreate/TaskUpdate como panel de progreso visible en sidebar de Claude Code.

## Preguntas abiertas

Sin preguntas nuevas de esta sesión.

## Próximos pasos

- Discutir e implementar Fase Beta: módulo de cookies en UI (`ui/src/lib/cookies.ts`)
- Fases Gamma-Delta: backend de invitaciones (token + endpoints `/auth/invite` y `/auth/register`)
