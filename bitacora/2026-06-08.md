# Bitácora — 2026-06-08

## Sesión: task/rename-model-to-spanish-22 (continuación)

## Resumen

Sesión de continuación del ticket #22. Se completaron las decisiones de diseño del modelo que habían quedado pendientes tras el compact del día anterior: simplificación de `AlumnoCarrera`, renombre de relaciones en `Materia`/`Correlativa`, y adición de nuevas LKPs y campos a `Materia` y `PlanDeEstudio`. Se commitearon los archivos eliminados y se generó y corrigió la migración inicial.

## Trabajo realizado

### proyecto-finisterre

Sin cambios.

### academika

**Decisiones de diseño sobre el modelo (sin commit aún):**

- **`AlumnoCarrera`**: se eliminó `carrera_id` (era redundante con `plan.carrera_id` y no existe el caso borde de alumno sin carrera en datos de Guaraní). `plan_id` pasa a NOT NULL y forma la PK compuesta con `alumno_id`. La relación `carrera` fue eliminada de `Carrera` también.
- **`Correlativa`**: se eliminó `plan_id` del modelo — las correlativas son por materia, no por plan (decisión basada en revisión del plan LI). `prerequisite_id` renombrado a `requisito_id`, relación a `requisito`. `_upsert_correlativas` simplificado: ya no requiere lookup de planes/carreras.
- **Nuevas LKPs**: `LkpNucleoCarrera` (`basico`, `avanzado`, `orientacion`) y `LkpNivelCarrera` (`grado`, `pregrado`).
- **`Materia`**: agregados `nucleo` (FK nullable), `horas_semanales`, `carga_horaria_total`. Relaciones renombradas: `correlativas_como_materia` → `materias_requeridas`, `correlativas_como_requisito` → `requerida_por`.
- **`PlanDeEstudio`**: agregado `nivel` (FK nullable a `lkp_nivel_carrera`). Eliminada relación `correlativas`.
- **Tests**: `test_students.py` actualizado para AlumnoCarrera con plan_id. Unit tests del importer actualizados (skip cuando plan no encontrado). Seeds de LKP en conftest actualizados.

**Commit realizado:**

- `6e35bee` — `chore(models): eliminar modelos, schemas, servicios y routers en inglés` (29 archivos, 1136 líneas)

**Migración:**

- Generada `2026-06-08_e5b7cb3a672d_schema_inicial.py`
- Corregida manualmente: eliminados 10 `op.drop_table()` espurios del `upgrade()`, agregados `lkp_user_roles` y `users` que faltaban (ya existían en el DB cuando autogenerate corrió)
- `make alembic-upgrade` falló por tablas viejas inexistentes → fix aplicado, pendiente de correr

### Workspace / configuración

Sin cambios.

## Decisiones tomadas

- **`AlumnoCarrera` sin `carrera_id`**: en datos de Guaraní, todo alumno inscripto tiene plan. `plan_id` NOT NULL es la fuente de verdad. La relación carrera se deriva via join.
- **Correlativas por materia**: mismo plan no cambia los requisitos de una materia. Simplifica el modelo y el importer.
- **`requisito`** como nombre del atributo en `Correlativa` (vs `prerequisite`, `correlativa`, `materia_correlativa`).
- **`materias_requeridas` / `requerida_por`** como nombres de las relaciones en `Materia`.
- **`anio_plan` se mantiene** en `Cursada` — fidelidad histórica del CSV, alumno puede cambiar de plan a mitad de carrera.

## Preguntas abiertas

- ¿`horas_semanales` y `carga_horaria_total` están disponibles en los CSV de Guaraní? (campos nullable por ahora)
- ¿`abbreviation` → `sigla` en `Materia` a confirmar con Eugenio

## Próximos pasos

1. `make alembic-upgrade` (migración ya corregida)
2. Segunda migración en blanco para seeds de LKPs (`make alembic-revision MSG="seed_lkp"`)
3. `make test && make test-integration`
4. Terminar review y commitear todo lo restante
5. Push + PR
