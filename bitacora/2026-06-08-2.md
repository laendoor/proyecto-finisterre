# Bitácora — 2026-06-08 (sesión 2)

## Sesión: task/rename-model-to-spanish-22 — cierre

## Resumen

Sesión de continuación del mismo día para cerrar el ticket #22. Se terminaron los commits pendientes del rename completo, se diseñó e implementó el mecanismo de seeds, se resolvieron 6 tests de integración rotos y se abrió y mergeó el PR #23. También se documentó la decisión en ADR-006.

## Trabajo realizado

### proyecto-finisterre

- Creado **ADR-006** (`ADR-006-modelo-de-datos-en-castellano.md`): documenta la decisión de usar castellano en el modelo de dominio, la frontera explícita con la capa de parsing (inglés), y las consecuencias positivas y negativas.

### academika

**Commits de la sesión:**

- `6e35bee` — `chore(models)`: eliminar 29 archivos en inglés (modelos, schemas, servicios, routers)
- `dd133ae` — `refactor(models)`: rename completo a castellano — modelos, schemas, servicios, routers, importador, tests, migración inicial regenerada. Incluye decisiones de modelo: `AlumnoCarrera` sin `carrera_id` (PK `alumno_id+plan_id`), `Correlativa` por materia (sin `plan_id`, campo `requisito_id`), nuevas LKPs `lkp_nucleo_carrera` / `lkp_nivel_carrera`, campos `creditos` / `nucleo` / `horas_semanales` / `carga_horaria_total` en `Materia`.
- `3f60a9c` — `feat(seeds)`: mecanismo de seed en dos tiers. Seeds de referencia en SQL puro (`sql/reference/01_lkp.sql`), runner Python thin que ejecuta archivos en orden alfabético. `make seed` (prod-safe) y `make seed-dev` (local). Agrega `desaprobado (D)` a `lkp_estado_cursada`.
- `75cde3c` — `fix(tests)`: tres grupos de fallas post-rename resueltos: `AlumnoCarrera` sin import y con `carrera_id` en `test_models`; ruta `/api/v1/planes-de-estudio` vs `/api/v1/planes` en `test_students`; planes 2020/2021 faltantes en seed del importer.
- `0105307` — fix menor en TRUNCATE de `conftest.py` (Lean, local).

**PR #23** — mergeado. Cierra issue #22.

### Workspace / configuración

Sin cambios.

## Decisiones tomadas

- **ADR-006**: modelo de dominio en castellano; tipos internos de parsing mantienen inglés como frontera explícita.
- **Seeds en SQL**: preferidos sobre Python por legibilidad y ejecutabilidad directa con psql. Un archivo por tier (`01_lkp.sql`); el prefijo numérico garantiza orden cuando el volumen crezca.
- **`seed-dev` corre `reference` primero** para garantizar integridad referencial en los seeds de muestra.

## Preguntas abiertas

- ¿`horas_semanales` y `carga_horaria_total` están disponibles en los CSV de Guaraní? (nullable por ahora)
- `sigla` en `Materia` — confirmar nombre con Eugenio cuando lleguen cabeceras reales

## Próximos pasos

1. Próximo ticket: **#7 — Auth — invitaciones** (`/fini-ticket 7`)
