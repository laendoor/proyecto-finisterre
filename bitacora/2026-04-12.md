# Bitácora — 2026-04-12

## Sesión: roadmap + cierre de anon_guarani

## Resumen

Sesión dividida en dos arcos. El primero cerró el trabajo pendiente de `anon_guarani.py`: refactor del flujo interactivo para reducir burocracia (auto-detección de columnas sensibles), corrección de errores de Pyright, y mejora de los datos de prueba con fixtures sintéticos realistas. El segundo arco fue el armado del ROADMAP de Académika: definición de las 5 fases + cierre, arquitectura de Hari (Text-to-SQL primero, RAG después, comparativa en Fase 5), y actualización del Plan de Trabajo para reflejar el nuevo diseño.

## Trabajo realizado

### proyecto-finisterre

- **Plan-de-Trabajo.md** actualizado para reflejar la nueva estructura de fases:
  - Fase 3: Hari con Text-to-SQL
  - Fase 4: RAG con embeddings
  - Fase 5: comparativa de enfoques
  - Cierre: validación + informe + defensa (lo que antes era Fase 4 + 5)
  - Duración actualizada de 16 a 20 semanas (~5 meses)
  - Objetivo específico 5 reescrito para mencionar ambos enfoques de Hari
- Subido a Drive ✓ — versión para la reunión del 17/04 con Gabi
- `.gitignore` agregado al repo

### academika

- **`anon_guarani.py`** refactorizado:
  - Nueva estrategia `anio` para columnas de año suelto (ej: `anio_ingreso`)
  - STRATEGIES actualizado a 3-tupla `(label, desc, fn)` — las descripciones se muestran en el selector
  - Auto-detección de columnas sensibles (`_KNOWN_SENSITIVE`) basada en headers reales de SIU Guaraní (nachoyegro como referencia)
  - Heurística `nombre`: solo se anonimiza si `apellido` también está presente (distingue nombre de persona vs nombre de materia)
  - Auto-selección de ref_id para fechas (legajo > dni > pregunta)
  - Flujo reducido: auto-detectar → mostrar → preguntar solo por columnas restantes → preview + confirmar
  - Correcciones de Pyright (standard mode): `fmt_used: str | None`, `StrFn = Callable[[str], str]`, `# pyright: ignore` para InquirerPy
- **`pyproject.toml`** actualizado: `pandas-stubs` + `pyright` como dev deps, `[tool.pyright]` con `typeCheckingMode = "standard"`
- **`datos-prueba/`** migrado desde `proyecto-finisterre/` a `academika/` — vive cerca del código
  - CSVs de nacho reemplazados por datos sintéticos realistas generados con `gen_datos_prueba.py`
  - 10 estudiantes (5 TPI, 5 LDS), 211 filas de trayectorias, inscripciones actuales
- **`gen_datos_prueba.py`** creado — generador de fixtures sintéticos (dev only, no se entrega)
- **`ROADMAP.md`** creado — roadmap interno de producto:
  - Fases 1-2: app base + analytics/Studio
  - Fase 3: Hari con Text-to-SQL (primera versión funcional)
  - Fase 4: RAG con embeddings sumado a Hari
  - Fase 5: comparativa Text-to-SQL vs RAG, decisión del approach definitivo
  - Cierre: validación con directores + informe + defensa
  - Timeline tentativo: ~5 meses (abril–septiembre 2026)

### Workspace / configuración

- **Prettier** configurado como formatter de Markdown en VS Code (reemplaza markdownlint para formateo)
- **`.prettierrc`** agregado al workspace (`proseWrap: preserve`, `printWidth: 120`)
- **Hook PostToolUse** (`.claude/hooks/format-markdown.sh`): formatea `.md` con Prettier y agrega lenguaje `txt` a bloques de código sin especificar (MD040) después de cada Write/Edit
- **`.vscode/settings.json`**: `python-envs.terminal.activateEnvironment: false` para evitar auto-activación del venv en terminales de VS Code

## Decisiones tomadas

- **Arquitectura de Hari:** Text-to-SQL como enfoque primario en Fase 3 (más preciso para datos tabulares/relacionales); RAG con embeddings incorporado en Fase 4; comparativa con decisión en Fase 5. Ningún enfoque descartado a priori — la evidencia decide.
- **datos-prueba en academika/:** los fixtures son insumo de desarrollo, no documentación académica.
- **anon_guarani columnas conocidas:** el mapa de auto-detección se basa exclusivamente en headers reales de las planillas del SIU Guaraní (nachoyegro como referencia); si aparecen nuevas planillas se ajusta.

## Preguntas abiertas

- Roadmap pendiente de revisión con Gabi (17/04) — es interno hasta entonces
- Plazo exacto de entrega a la Comisión de Investigación (Gabi lo confirma)
- Datos de Guaraní reales — qué tan incompletos están (a confirmar con Eugenio/Denise)
- Alcance exacto del POC para validación con directores

## Próximos pasos

- Reunión con Gabi 17/04 a las 5:30pm — Plan de Trabajo en Drive ✓
- Armar encuesta para Eugenio y Denise (qué datos tienen, qué preguntas quieren responder)
- Arrancar Fase 1 una vez validado el plan con Gabi
- README de academika (pendiente, archivo vacío)
